# config file for generating embeddings
#
# --- Data set Paths ---
#
path_to_load: "/home/nvidia/workplace/k_distil_res_second_run/run_20260318_154848/finetune_20260329_170424/"
path_to_data: "/kaggle/input/datasets/torshamajumder/abla-6k-proxy-data/ablation_data_astra-distil_6K/train/"
path_to_save: "/kaggle/working/supervised_bilstm_20260910_124124/train-emb/"
path_to_class_count: "/kaggle/input/datasets/torshamajumder/abla-6k-proxy-data/ablation_data_astra-distil_6K/train/data_stats.csv"
#
# --- Model Parameters ---
#
num_layers: 4          # Number of encoder layers
mjd: True              # Use MJD for positional encoding
base: 10000.0          # Base for positional encoding
d_model: 256           # Embedding dimension
num_heads: 4           # Number of attention heads
dff: 1024              # Feed-forward network dimension
rate: 0.1              # Dropout rate
use_band_info: True    # Use segment embeddings
time_scaling: 904.78   # Scaling factor for time indices in scaled positional encoding (288 * np.pi)
use_drop: True        # Use dropout
projection_dim: 2048    # Projection head dimension 
#
# --- Inference parameters ---
#
model_type: "cnn"          # Choose between 'cnn' or 'bilstm' for the supervised model architecture
finetune: True      # Flag to load best finetuned weights for generating embeddings
batch_size: 10       # per-GPU Batch size (adjust based on GPU memory)/ Just a batch size for CPU training
num_gpus: 1          # # of GPU instances to use for training. Set to 0 for CPU training. (Default set to 0.)
global_view_maxlens: {'g': 250, 'r': 300, 'i': 50} # Using ANCHOR view sequence length, i.e., build_seg_len
#
# --- Required only when finetuning is True ---
unfreeze_layers: "all"      # unfreeze the last 2 encoderLayers
# --- Label Map for the dataset used for finetuning ---
# --- Else can be ignored. --
#
label_map: {
            'AGN': {
                    'count': 4,
                    'id': 0
                },
            
            'DSCT|GDOR|SXPHE': {
                    'count': 4,
                    'id': 1
                },
            'ECL': {
                    'count': 4,
                    'id': 2
                },
            'LPV': {
                    'count': 4,
                    'id': 3
                },
            'RS': {
                    'count': 4,
                    'id': 4
                },
            'S': {
                    'count': 5,
                    'id': 5
                },
            'SOLAR_LIKE': {
                    'count': 5,
                    'id': 6
                },
            'YSO': {
                    'count': 4,
                    'id': 7
                },
            'CEP': {
                    'count': 4,
                    'id': 8
                },
            'CV': {
                    'count': 4,
                    'id': 9
                },
            'ELL': {
                'count': 4,
                'id': 10
            },
            'RR': {
                    'count': 4,
                    'id': 11
                }
            }


